import { IActionDefinition } from '../../../api';
import { ActionType } from './ActionType';
export declare class ActionDefinition implements IActionDefinition {
    private _id;
    private _state;
    private _precedence;
    private _activePartSet;
    private _assetPartDefinition;
    private _lay;
    private _geometryType;
    private _isMain;
    private _isDefault;
    private _isAnimation;
    private _startFromFrameZero;
    private _prevents;
    private _preventHeadTurn;
    private _types;
    private _params;
    private _defaultParameterValue;
    private _canvasOffsets;
    constructor(data: any);
    setOffsets(k: string, _arg_2: number, _arg_3: number[]): void;
    getOffsets(k: string, _arg_2: number): number[];
    getType(id: string): ActionType;
    getParameterValue(id: string): string;
    getPrevents(type: string): string[];
    private getTypePrevents;
    getPreventHeadTurn(k: string): boolean;
    isAnimated(k: string): boolean;
    get id(): string;
    get state(): string;
    get precedence(): number;
    get activePartSet(): string;
    get assetPartDefinition(): string;
    get lay(): string;
    get geometryType(): string;
    get isMain(): boolean;
    get isDefault(): boolean;
    get isAnimation(): boolean;
    get startFromFrameZero(): boolean;
    get prevents(): string[];
    get preventHeadTurn(): boolean;
    get params(): Map<string, string>;
}
