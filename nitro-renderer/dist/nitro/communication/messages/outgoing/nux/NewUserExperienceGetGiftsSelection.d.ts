export declare class NewUserExperienceGetGiftsSelection {
    private _dayIndex;
    private _stepIndex;
    private _giftIndex;
    constructor(dayIndex: number, stepIndex: number, giftIndex: number);
    get dayIndex(): number;
    get stepIndex(): number;
    get giftIndex(): number;
}
